---
id: CC-039
title: Mainnet go-live gate checklist (hub)
status: todo
priority: P1
area: infra
epic: mainnet
linear: NOR-306
created: 2026-07-25
updated: 2026-09-16
---
# CC-039 — Mainnet go-live gate checklist (hub)

Migrated from NOR-306, which was the hub issue — nearly every other open Linear issue fed
into it. Treat this as the mainnet readiness source of truth and keep it current.

## Gates
**Prerequisite** — `CC-076` (Sepolia validation gate) must be done before any of the below
starts. `CC-034` already said this explicitly ("do not start until the whole of CC-076 is
clear" — corrected 2026-08-26 from the stale CC-032/CC-035 phrasing). Item-level status of
that gate is re-derived on `CC-034`'s 2026-09-16 update: the lifecycle stages
(`CC-032`/`CC-077`/`CC-078`/`CC-079`), `CC-072`, `CC-036`, `CC-033`, `CC-040` (day-of),
`CC-048`, `CC-084` (in-progress), `CC-041` (blocked) and the day-of security sweep are what
stand between now and "CC-076 clear".
**Contract** — deploy to Base mainnet, Slither scan, Basescan verify (CC-034). Note
`ADR-0006` D2/D3 are now scoped **into** the deploy itself (2-of-4 Safe owner, arbitration
clock, `completeTaskByOwner` already in source per CC-034's 2026-09-16 update) — the
contract gate is no longer just "run the deploy script". `CC-090`'s Sepolia half
(signer/owner separation) is mid-flight; its mainnet legs sequence after the deploy
transaction, before any funding.
**Security (mainnet-specific), day-of** — run the full OWASP Top 10 web sweep and the Web3
attack-vector sweep across the project, resolve every CRITICAL and HIGH, document MEDIUMs as
resolved or explicitly accepted with a rationale, attach the report for the audit trail, and re-run
`npm audit`. Run this *immediately before* the mainnet cutover, not in advance — a sweep goes stale
as soon as anything else changes. Do not assume the Sepolia pass still holds after however long the
site has been sitting behind the coming-soon gate on testnet, and note the risk profile is different
now that the money is real. (This was tracked as `CC-035`, closed `wontfix` on 2026-08-11 because it
is a launch-day procedure rather than a defect; the scope lives here and in `CC-076`.) Fold in the
non-npm-specific items from `CC-045` while you are here: the
`child_process`/`exec`/`spawn`/`eval` audit and Zod coverage on every MCP tool input.
**CC-045 closed 2026-09-16** (PR #208) with exactly those items executed — its audit table is
the baseline the day-of sweep updates. Its SSRF residue was spun out as `CC-108` (**open**,
P2) — schedule into the day-of sweep rather than leaving it floating.
**Infrastructure** — Cloudflare WAF review, Vercel env var audit (repoint every `NEXT_PUBLIC_*`
contract address and network var to mainnet). The full set read by `src/` at build time, as
of 2026-09-16: `NEXT_PUBLIC_ESCROW_CONTRACT`, `NEXT_PUBLIC_REPUTATION_STAKE_CONTRACT`,
`NEXT_PUBLIC_USDC_ADDRESS`, `NEXT_PUBLIC_BASE_NETWORK`, `NEXT_PUBLIC_BASE_URL`,
`NEXT_PUBLIC_COMING_SOON`, `NEXT_PUBLIC_INTAKE_PAUSED`,
`NEXT_PUBLIC_INTAKE_PAUSE_NOTICE`, `NEXT_PUBLIC_TIP_WALLET_ADDRESS` (**still a testnet
value** — CC-031's tip jar is unmounted but kept, and this var must be repointed to a
mainnet address before the jar is ever remounted). `NEXT_PUBLIC_STABLES_AFFILIATE_URL` is
dead (removed by CC-029; nothing reads it). Env-var truth lives in `.env.example` and
`chain-constants.json` — verify against those, not this list.
**Content / business** — Security-Trust-Disclosure.md current for mainnet, confirm /learn and
the homepage no longer carry testnet-specific language once real money is live, plus **the
content-pass inventory below** — deferred work other tickets explicitly routed here: tip jar
remount + connected-wallet revisit (`CC-031`), OG card-debugger validation on the first live
public URL (`CC-012`), live robots/sitemap crawl validation (`CC-013`), README rewrite for
post-mainnet reality + the registry-listing/npm decision (`CC-014` items). Both `CC-099`
(done) and `CC-098` (open) have compliance-copy lines in this pass — the
Security-Trust-Disclosure sanctions section ships with the site either way, but re-read the
published compliance copy once the money is real.
**Smoke test (mainnet, with real funds)** — full fund → complete → pay lifecycle, plus dispute
flow, run by Aaron with his own money before anyone else's. This is the "re-test everything with
my own funds" step, and nothing after it is allowed to assume the Sepolia results still hold.
**CC-099 is now a satisfied prerequisite of this gate** — its closing note asked for exactly
this line; done 2026-08-23 (PR #136). Its monitor runs via `run-monitors.mjs` and **SKIPs
visibly until the repo secrets (`SUPABASE_URL`, `SUPABASE_ANON_KEY`) are set — setting them
is day-of readiness, not optional**. **CC-098 (AUSTRAC classification) remains open** and
gates this smoke test per the Regulatory line.
**Public launch** — `CC-014` (flip `NEXT_PUBLIC_COMING_SOON`, redeploy, verify). This is the
literal last gate in the whole backlog: it does not run until the mainnet smoke test above is
clean. The public sees the site for the first time here, on mainnet, not on Sepolia. CC-014's
2026-09-16 pre-flip audit consolidated its checklist; four boxes there remain unticked
(README rewrite, registry/npm decision, reputation-seed check, x402 sweep `CC-103`) and
**all four are post-mainnet by definition** — they belong on this hub's content pass and do
not block the flip.
**Post go-live** — 48-hour watch checks
**Regulatory (added 2026-08-18)** — `CC-098` (AUSTRAC AML/CTF Tranche 2 classification, a lawyer
question with its obligation window already open, not a 2027 one) and `CC-099` (sanctions/PEP
wallet screening, shippable regardless of `CC-098`'s answer) both resolved *before* the mainnet
smoke test above, not after. The smoke test is real funds moving on a real designated-service
question — see `CC-098`'s reasoning on why even a self-funded test may be the trigger event.
**Status 2026-09-16: `CC-099` done** (PR #136, 2026-08-23). **`CC-098` still open** — it is the
one regulatory item between now and the mainnet smoke test, and the long pole.
`CC-110` (diarised ASIC/DAP review, from `CC-051`) is the same single lawyer engagement's other
half — pair it with `CC-098` (the AUSTRAC instruction brief
`docs/legal/CC-098-austrac-vasp-lawyer-brief.md` is the vehicle) rather than briefing twice, but
it does not gate the smoke test on its own: its commencement clock runs to 2027-04-09.

## Known staleness to reconcile
NOR-306's own checklist still showed NOR-196, NOR-301 and NOR-195 as unchecked even though
NOR-196 and NOR-185 were marked Done elsewhere. Do not trust the inherited tick-boxes —
re-derive each gate's status from the code and this backlog before treating it as
authoritative.

---

## Update 2026-08-10 — split from the Sepolia validation gate

This hub had been carrying gates for two different milestones — everything provable on Sepolia,
and moving to Base mainnet — which made it unclear which items were actually blocking the *next*
thing. Moved everything that's really about Sepolia validation (`CC-041`, `CC-032`, `CC-040`,
`CC-029`, `CC-007`, plus several found and reclassified this session — `CC-035`, `CC-037`,
`CC-044`, `CC-045`, `CC-046`, `CC-036`) out to the new `CC-076`. What remains here is genuinely
mainnet-only: things that cannot happen, or cannot be meaningfully verified, before a mainnet
deployment exists at all. `CC-043`, `CC-042`, `CC-049` were already `done` and are removed from
the active gate list rather than left as stale references.

## Update 2026-08-10 (2) — the coming-soon gate flip belongs here, not in CC-076

Aaron confirmed the actual sequence explicitly: build and test everything on Sepolia first (`CC-076`)
→ switch to mainnet and re-test the full lifecycle with real personal funds (this issue) → only
once that's clean does the site open to the public. `CC-014` (the coming-soon gate flip) had been
listed as `CC-076`'s last step, which implied the public sees the site while still on Sepolia.
That's backwards from Aaron's stated plan — moved `CC-014` here as the final gate, run only after
the mainnet smoke test above passes. `CC-076` finishing means "ready to attempt mainnet," not
"ready to go public."

---

## Update 2026-08-11 — backlog triage pass

Reviewed in the triage pass — **stays P1**, structure confirmed correct. `CC-035` now
appears as an explicit gate line here rather than as a separate open issue (it was closed
`wontfix` for that reason), and `CC-012`, `CC-013` and `CC-031` were lowered to P2 with the
reasoning that they belong in this hub's content pass rather than `CC-076`.

`CC-007` is now `blocked` behind `CC-034` and is expected to close as `wontfix` once mainnet
lands — see its note. The practical consequence for this hub: the "confirm /learn and the homepage no
longer carry testnet-specific language" gate should also confirm that no testnet-pilot language was
ever *added*, because the decision was not to add it.

---

## Update 2026-09-16 — hub reconciliation against the repo (this run)

The card landed while the gate is still red, so this run started from the ticket's own rule —
re-derive, don't inherit. Status re-derived against `docs/backlog/` and the kanban board at
2026-09-16 (master d6d709d). The gate text above was rewritten where facts had moved; this
section records what changed and why.

### Gate status, re-derived

- **Prerequisite CC-076: not clear.** Still open: CC-032/CC-077/CC-078/CC-079 (lifecycle),
  CC-072, CC-036, CC-033, CC-040 (day-of), CC-048, CC-084 (in-progress), CC-041 (blocked),
  day-of security sweep. CC-034's same-day update carries the same list.
- **CC-045 done (2026-09-16, PR #208)** — the "fold in CC-045" instruction is now historical;
  the fold-in items were executed in that PR. Residue: `CC-108` (webhook SSRF, open P2) —
  scheduled into the day-of security sweep.
- **CC-099 done (2026-08-23, PR #136)** — now recorded as a satisfied prerequisite on the
  Regulatory gate line, per the gate line CC-099's own closing note asked for. Its monitor
  runs via `run-monitors.mjs`; the repo secrets are not yet set — day-of readiness includes
  setting them.
- **CC-098 open** — unchanged since 2026-08-18; it gates the mainnet smoke test and needs a
  lawyer, not a merge. It is the long-pole regulatory item; flagged for PO attention.
- **ADR-0006 D2/D3 now bytecode-scoped into CC-034's deploy** — the Contract gate text was
  updated to say so. The arbitration clock, 2-of-4 Safe owner and `completeTaskByOwner` are
  no longer future work; they are properties of the bytecode that ships.
- **CC-090's Sepolia half is mid-flight** — the Contract gate now carries the sequencing
  note (mainnet legs after the deploy transaction, before any funding).

### Data corrections shipped in this run

- **`chain-constants.json` `deployedAt` was stale** — read `2026-08-15` (the superseded
  pre-D3 deploy) for a deployment that actually went live 2026-09-01. Corrected to
  `2026-09-01` with provenance in the `$comment`. This stale field was the sole basis of
  CC-034's 2026-09-16 finding #2 ("live escrow predates the D3 bytecode"), which is
  **withdrawn** by correction on CC-034 in the same commit. On-chain evidence:
  `ARBITRATION_WINDOW` reads 604800s on `0xc6aa…E4d3`, and deployBlock 46227900's timestamp
  is 2026-09-01T00:54:48Z — the address did not exist on 2026-08-15.
- **Env-var inventory for the Vercel audit** — the Infrastructure gate now lists every
  `NEXT_PUBLIC_*` var read by `src/` as of 2026-09-16 (grep-derived), including
  `NEXT_PUBLIC_TIP_WALLET_ADDRESS`, which CC-034's list never named and which is still a
  testnet value. `.env.example`/`chain-constants.json` remain the authority for the full
  set.

### The deferred-work inventory (new)

Six tickets explicitly defer work to "CC-039's content pass" and none of it was inventoried
on this hub. Now a single list under the Content/business gate line:

1. CC-031 — tip jar remount + revisit the connected-wallet requirement; repoint
   `NEXT_PUBLIC_TIP_WALLET_ADDRESS` to mainnet first.
2. CC-012 — OG card-debugger validation against the first live public URL;
   `metadataBase`-into-`config.ts` if a site-URL env var ever appears.
3. CC-013 — live robots.txt/sitemap.xml crawl validation once anything is public.
4. CC-007 — verify no testnet-pilot language was ever added (re-verified clean 2026-09-16
   by CC-014's audit; re-check at content-pass time).
5. CC-014 — README rewrite for post-mainnet reality; the registry-listing/npm decision on
   the `basedhuman-mcp` placeholder (decision owed, recorded on CC-014).
6. CC-099/CC-098 compliance copy — Security-Trust-Disclosure.md is current, but re-read
   the published compliance copy once the money is real.

### Board routing notes

- The kanban cards for CC-076/CC-039 were traced to the repo; both hubs read `todo` in the
  repo and the board. No card/board mismatch to reconcile on this run.

### Not done here, deliberately

- **CC-098 is not resolvable by merge.** It needs a lawyer's answer. It stays the
  long-pole gate on the smoke test and is flagged for PO attention.
- **No test additions.** `chain-constants.test.ts` only cross-checks derived values, and
  `deployedAt` is not one — verified before editing, so no test change was owed.
- **CC-109's PO-provisioning work is its own ticket;** not folded into this hub.
