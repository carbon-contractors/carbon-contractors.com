---
id: CC-034
title: Mainnet deployment run
status: todo
priority: P1
area: infra
epic: mainnet
created: 2026-07-25
updated: 2026-09-16
---
# CC-034 — Mainnet deployment run

Section 3 of the MVP definition of done. **Do not start until the whole of `CC-076` (the Sepolia
validation gate) is clear.** This deliberately points at the hub rather than at named issues — it
previously read "do not start until CC-032 and CC-035 are green", which went stale within weeks when
`CC-035` became a day-of checklist line and `CC-032` was split four ways.

Fresh deployer wallet never used on testnet; deploy escrow to Base mainnet; authorise the
KMS-derived signer address on the contract; verify the derived address matches the contract
owner on Basescan; repoint every `NEXT_PUBLIC_*` contract address, `NEXT_PUBLIC_BASE_NETWORK`,
`NEXT_PUBLIC_BASE_URL` and `NEXT_PUBLIC_USDC_ADDRESS` to mainnet; confirm monitoring
(escrow balance, signer gas, function logs, Supabase logs).

Then one real small-value transaction end to end before any public announcement.

Note the KMS/HSM signer work is already done and tested (`src/lib/contracts/kms-signer.ts`,
`scripts/verify-kms-signer.ts`, attestation bundle in `docs/`), so the old raw-key blocker
is closed. Confirm `DEPLOYER_PRIVATE_KEY` is absent from all deployed environments.

---

## Update 2026-07-25

CC-039 is the consolidated go-live gate (from NOR-306) — treat that as the checklist and this as
the execution step.

Add to the deploy scope: the export also calls for a **Slither scan** and **Basescan source
verification** of the mainnet contract, neither of which was in the original checklist here.

And per CC-049, the mainnet deploy is the natural window to add a `completeTaskByOwner(taskId)`
emergency function — the current `Ownable` design leaves in-flight tasks stuck if the signer key
rotates, and a new deployment is the only cheap chance to fix it.

---

## Update 2026-08-26 — two `ADR-0006` decisions are now bytecode-or-never for this deploy

`ADR-0006` was accepted 2026-08-26 with execution parameters. Two of them are scoped **into this
milestone** rather than after it, because both are one-way doors that a redeploy is the only way to
reopen.

### 1. Arbitration deadline, in the bytecode (D3)

A disputed task currently has **no clock**. `resolve_dispute` was removed from MCP on 2026-08-26
(`ADR-0001` D2, PR #146), so arbitration is owner-only via `scripts/admin/`, and if the owner never
acts the task sits `Disputed` indefinitely with the escrow held.

D3, as corrected by **Amendment 1** (2026-08-26) — read that before implementing, because the
mechanism in D3's original text does not work:

- **`ARBITRATION_WINDOW = 7 days`, a contract constant.** Not a settable window: the arbitrator must
  not choose its own deadline, which is the structure `ADR-0001` D2 removed.
- **The clock runs from `disputeTask`, not `beginArbitration`.** `beginArbitration` is `onlyOwner`
  and the contract documents `Disputed → Arbitrating` as "a marker, not a gate" — so an owner who
  never calls it would never start a clock, and the task would sit `Disputed` forever. That is the
  stranding case this decision exists to close. `disputeTask` is callable by either party, so the
  clock cannot be withheld.
- Window passes with no `resolveDispute` → the task becomes claimable **by the worker**, on the same
  pull-payment mechanism as `releaseAfterReview`.
- **Worker-default, not refund-default.** `ADR-0001` D6's reasoning carries over: a refund-default
  hands the platform a griefing lever it exercises by doing nothing, and would mean the operator's
  death pays the agent.

**Bytecode scope:**

- `uint64 disputedAt` on `Task`. Slot 1 is 30 of 32 bytes used so this takes a new slot; only
  `disputeTask` writes it, so `createTask` never pays for it.
- A worker-claim path for the timeout, reusing `_payOut` with a new `CompletionRoute` member
  **appended** (safe — the enum is only read from events).
- Tests on the boundary in both directions, and specifically **a dispute where `beginArbitration` is
  never called must still time out**. That test is the one that proves A1.1 rather than restating it.

**Worst-case post-delivery wait becomes `MAX_REVIEW_WINDOW` + 7 days = 21 days**, and only when an
agent chose the maximum review window, disputed, and let arbitration run full length.

**In bytecode, not in a scheduled job.** A clock enforced by a cron is a clock that stops when the
operator does — which is the exact scenario `ADR-0006` exists for. `ADR-0001` A1.1 removed platform
liveness from settlement; a runbook-enforced deadline would put it straight back.

**Blocked on one thing:** the bound values. They are an `ADR-0006` open item and must be chosen
before this bytecode is frozen. Everything else about D3 is decided.

### 2. Owner is a 2-of-4 Safe, and is not the verdict signer (D2, `CC-090`)

Today one HSM key owns `CarbonEscrow` **and** is its only accepted verdict signer. Both change before
this deploy:

- **Owner → a 2-of-4 Safe** over four hardware-isolated keys: each Tangem card initialised **on its
  own**, link step not run (`ADR-0006` A2). Custody is decided — Aaron holds two in two separate
  buildings, two family members hold one each, and **the two family keys reach threshold alone**,
  which is what makes succession work without an estate locating anything.
- **Verdict signer stays a single HSM key.** It must sign automatically and holds no custody; losing
  it fails safe, because no verdicts means everything resolves to workers after the review window.

**Four cards are only four keys if each is initialised on its own.** Cards linked to one key are one
key wearing several plastic coats, and it looks exactly like a real multisig on Basescan.

*Corrected 2026-08-31 (`ADR-0006` A2). This said the trap was silent and that Tangem cards share one
seed by default. Neither is true: cards arrive uninitialised and linking one to another's key is an
explicit setup step, so a shared seed is a visible choice during setup.* **The acceptance test is
on-chain and not procedural** — four Safe owners at four addresses with no shared derivation, verified
before any value moves — plus reading the four addresses off the cards before they are distributed,
which costs nothing. Both are `CC-090` closing conditions.

**Hardware ordered 2026-08-31, ~A$130, expected ~2026-09-17.** Procurement is no longer the blocker on
this half; the family-only rehearsed signature and the on-chain owner check are (`CC-090`).

**And a sequencing hazard already recorded on `CC-082`:** a redeploy resets ownership to the deployer.
So the Safe must be made owner *after* deployment and *before* anything is funded, and
`verify-contract-owner.mjs` must be re-run against mainnet — not assumed from the Sepolia result.

### Consequence for this ticket

**Neither item can be retrofitted.** The arbitration deadline is bytecode; the ownership transfer is
cheap but its verification is the gate. If this deploy happens without D3, v1 ships with disputes that
can strand — and the fix is a second mainnet deploy with a migration, which is the thing this project
has spent the whole of `ADR-0001` avoiding.

Custody of the three Safe keys is an **open `ADR-0006` item and a blocker on D2's actual purpose** —
three cards held by one person is loss resistance, not succession.

Related: `ADR-0006` D2/D3, `CC-090`, `CC-039`, `CC-082`, `chain-constants.json` (`base-mainnet` block,
deliberately null until this runs).

---

## Update 2026-09-16 — pre-flight reconciliation + the `completeTaskByOwner` window closed

Aaron's card for this ticket landed while the gate is still red, so this run started with a full
reconciliation against the repo (cards say "P1 infra (mainnet)"; the repo is the source of truth).
Three findings, one piece of work shipped.

### 1. Gate audit — `CC-076` is not clear; the deploy itself stays blocked

Item-level status re-derived 2026-09-16 (not inherited tick-boxes), against `docs/backlog/` and the
kanban board:

- **Done since the 2026-08-11 triage:** CC-082, CC-080, CC-081, CC-083, CC-085, CC-086, CC-087,
  CC-088, CC-089, CC-070, CC-060, CC-029, CC-027, CC-021, CC-011, CC-046, CC-073, CC-074, CC-017,
  CC-022/023/024, CC-094, CC-095, CC-099, CC-045. The gate is far greener than its hub ticket's
  `updated: 2026-08-11` suggests.
- **Still open on `CC-076`:** CC-032 / CC-077 / CC-078 / CC-079 (the four lifecycle stages — this
  smoke test *is* the gate), CC-072 (staking, 7-day cooldown tail), CC-036, CC-033, CC-040 (day-of),
  CC-048, CC-084 (in-progress), CC-041 (blocked, needs_input), plus the day-of security sweep.
- **`CC-090`** is mid-flight: testnet half complete (address census, Safe rehearsal, three
  executed 2-of-4 pairs). Remaining before this deploy: ACR1552U rig, four access codes, custody
  assignment (family = cards 2+3 recommended — **Aaron's call, open `ADR-0006` item**). Mainnet
  legs sequence *after* this ticket's deploy transaction, before any funding.

**The deploy transaction itself therefore stays blocked** behind CC-076's lifecycle stages and
CC-090's custody decision. Nothing here advances that date; the point of this update is that the
date is now honest.

### 2. The live Sepolia escrow predates the D3 bytecode

`ARBITRATION_WINDOW` landed in source 2026-08-28 (cf87b4b, ADR-0006 D3 + Amendment 1). The live
Production Sepolia escrow (`0xc6aa…E4d3`) was deployed 2026-08-15 — **the deployed testnet contract
does not contain the arbitration clock**. When CC-077/CC-078/CC-079 run against it, they validate a
bytecode that is not the bytecode mainnet will receive. Either redeploy Sepolia from current source
before the lifecycle stages, or run them in the full knowledge that the D3 path is validated only by
the Solidity test suite (which does cover it, including the never-called-beginArbitration case).
Flagged to CC-076's gate list; decision is the PO's.

### 3. Shipped: `completeTaskByOwner(taskId)` — the CC-049 window, closed

`CC-049` scoped a `completeTaskByOwner(taskId)` emergency function into this deploy ("a new
deployment is the only cheap chance to fix it"); it existed nowhere in source. Implemented on the
mainnet-bound bytecode now (this PR) rather than day-of:

- Owner-only, `nonReentrant`, callable from `Funded` or `Delivered` — the same state gates as
  `completeTask`.
- Pays `task.worker` via `_payOut` with a new `CompletionRoute.OwnerEmergency` **appended** (the
  D3-sanctioned pattern; the enum is only read from events, and no app-layer code validates its
  range — verified by search). No arbitrary destination is reachable, owner included
  (ADR-0001 D9; re-stated for this exact function on CC-080).
- **Not** callable on `Disputed`/`Arbitrating`: those states belong to `resolveDispute` /
  `releaseAfterArbitration`, and an emergency path that could pre-empt an active arbitration would
  let the owner overrule a pending ruling. A disputed task does not need it — the arbitration clock
  already pays the worker on timeout.
- Six tests in `test/CarbonEscrow.ts`, including the CC-049 case itself (Delivered + agent key
  gone) and the D9 no-arbitrary-destination property. `escrow-abi.ts` regenerated (CI checks it).
  79/79 contract tests green.

This does not make the deploy runnable; it makes the bytecode complete so the deploy, when its
gates clear, is not held up by missing source. The remaining pre-deploy checklist (fresh deployer
wallet, Slither, Basescan verification, Safe transfer, env repoint, monitoring, one real
small-value transaction) is unchanged and lives on `CC-039`.