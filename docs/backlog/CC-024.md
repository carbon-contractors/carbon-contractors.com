---
id: CC-024
title: Hourly rate field gives Australian workers no AUD context
status: done
priority: P1
area: content
epic: public-launch
created: 2026-07-25
updated: 2026-08-21
---
# CC-024 — Hourly rate field gives Australian workers no AUD context

`src/app/connect/page.tsx:127-136` labels the field "Hourly Rate (USDC)" with placeholder
"e.g. 150". An Australian tradesperson thinking in AUD will mis-price by roughly 1.5x in one
direction or the other.

Fix: add helper text — "1 USDC ≈ 1 USD" plus a live-ish or clearly-dated indicative AUD
figure, and consider showing the AUD equivalent as they type.

---

## Update 2026-08-11 — backlog triage pass

Raised P2 → **P1**. The target market is Australian, so this is not a nicety: every
Australian worker registering without AUD context mis-prices themselves by roughly 1.5x, the wrong
rate is then stored permanently, and `src/lib/db/whitepages.ts` ranks and returns it to agents.
A cheap piece of helper text prevents a durable data-quality problem.

Same `/connect` pass as `CC-022` and `CC-023`.

## Resolution

Done 2026-08-21 on `feat/cc-022-023-024-connect-registration-polish`.

`src/app/connect/page.tsx`'s Hourly Rate field now has helper text —
"1 USDC = $1.00 USD ≈ $1.55 AUD indicative. Enter your rate in USDC." — and, as the worker
types a *valid* rate, a live equivalent ("≈ $232.50 AUD/hr indicative") shown under the input.
The AUD figure comes from a named constant (`INDICATIVE_AUD_PER_USDC = 1.55`) with a comment
pinning it as indicative, never a quoted rate. If the entered rate fails validation, the same
slot shows the CC-022 error message instead.

Note: copy deliberately says "indicative", not a promise of a rate — consistent with ADR-0004's
target-state posture. If a dated or live FX figure is wanted later, that is a follow-up, not
part of this fix.
