---
id: CC-076
title: Public launch go-live gate checklist (hub)
status: todo
priority: P1
area: infra
epic: public-launch
created: 2026-08-10
updated: 2026-09-16
---

# CC-076 — Sepolia validation gate: everything provable before attempting mainnet (hub)

## Why this exists

`CC-039` ("Mainnet go-live gate checklist") had been doing double duty as the checklist for two
different milestones: everything that can be built and proven on Sepolia, and the mainnet
migration itself. That made it hard to tell which items were actually blocking the *next* thing
versus a later one — `CC-034` (mainnet deploy) already says "do not start until the whole of
CC-076 is clear," which is exactly the split this issue makes explicit.

**The actual launch sequence (confirmed by Aaron, 2026-08-10):** build and test everything possible
on Sepolia (this hub) → switch to Base mainnet and re-test the full lifecycle with real personal
funds (`CC-039`) → only once that mainnet re-test is clean does the coming-soon gate lift for the
public. **This hub finishing does not mean the site goes public** — it means Sepolia is proven
enough to justify attempting the one-way mainnet migration. The actual gate flip (`CC-014`) lives
at the end of `CC-039`, after mainnet, not here.

As with `CC-039`, do not trust inherited tick-boxes — re-derive each gate's status from the code
and the backlog before treating anything here as authoritative, this list included.

## Gates

Rewritten 2026-09-16 by reconciliation against the repo (master 4a95c30) — the 2026-08-11 gate
list is preserved below the update log. Roughly two-thirds of that list has since closed,
including three of its four P0 blockers; what remains is almost entirely *execution proof*, not
build work. Sequencing note: the lifecycle stages want starting in order, because each proves
machinery the next consumes, and `CC-072` has a 7-day cooldown tail.

**Blockers — all four closed.** `CC-082` (CarbonEscrow v2 + Sepolia redeploy, live at
`0xc6aa…E4d3` since 2026-09-01 with the D3 arbitration clock confirmed on-chain), `CC-080`
(the hire→pay loop), `CC-081` (trust-boundary defects), `CC-037` (escrow trust boundary
re-verification, the `CC-051` evidence base). The v2 write paths the old list never mentioned
were also built and merged: `CC-092` (app-layer submitWork/verdict/claim), `CC-094` (offer and
accept, `ADR-0005` D2), `CC-093` (authenticated `/api/tasks`).

**Lifecycle execution — the core of what is left.** Nothing has yet been driven through the
product end to end against the v2 deployment:
- `CC-032` (P1) — **Discovery**: register → MCP discovery → correct worker data. Its prose happy
  path still describes the pre-`CC-081` x402 flow; the 2026-08-28 rescopes are authoritative.
- `CC-077` (P1) — **Funding**: offer/accept (`ADR-0005` D2) → `/api/fund-task` →
  `escrow.createTask` → worker notified. **There is no x402 payment challenge** — removed by
  `CC-081` Defect 1; an x402 settlement is a bare ERC-20 transfer, so paying the escrow address
  directly strands funds. `acceptance_spec` and `review_window_hours` are required tool arguments.
- `CC-078` (P1) — **Settlement**: rescoped 2026-08-28 to the worker pull-payment claim
  (`submitWork` → review window → agent verdict *or* `releaseAfterArbitration`); all three
  settlement routes, the wallet-change edge case, and `verify-escrow-solvency` after each.
- `CC-079` (P1) — **Dispute**: the mechanism is live-proven (`CC-059`, 2026-07-30, on-chain
  `TaskResolved` with USDC movement, Basescan-verified KMS sender) but the *product* path since
  `CC-065` has never been exercised, nor the refund outcome, nor `dispute_task` itself.
- Prereqs for all four are green: `CC-070` done (chunked `eth_getLogs`), `CC-060` done (test
  suite no longer broadcasts). What is *not* green: `BASE_SEPOLIA_RPC_URL` — see CC-104 below.

**New since the 2026-08-11 list**
- `CC-084` (P0, in-progress) — acceptance spec: schema, hashing, worker-facing display and the
  deterministic checker all merged (PR 1, PR 2 with `CC-093`, corrected 2026-08-26 when the
  phash check was found implemented backwards). Open on live proof through the funding lifecycle
  — exercised by `CC-077`, not by more code.
- `CC-104` (P0, in-progress) — invariant monitor alert classification. Merged in #201 with all
  acceptance boxes ticked; two PO-blocked residues: the one-line
  `EAS_SCHEMA_REGISTRY_ADDRESS` workflow addition (needs `gh auth refresh -s workflow`, on
  branch `fix/cc-104-monitors-env`) and the `BASE_SEPOLIA_RPC_URL` repo secret (needs a real
  provider credential). Until the secret lands, the TRAN class persists at public-endpoint rates.
- `CC-085`/`CC-086`/`CC-087`/`CC-088`/`CC-089`/`CC-083` — all done; the monitors exist and run
  in CI (`monitors.yml`), the kill switch and runbooks shipped with them.

**Security**
- Day-of security sweep — OWASP Top 10 + Web3 attack-vector sweep, resolve every CRITICAL and
  HIGH, document accepted MEDIUMs, re-run `npm audit`. Run *last*, immediately before the gate
  clears. `CC-045` closed 2026-09-16 (PR #208) with the `child_process`/`exec`/`spawn`/`eval`
  audit and MCP-input Zod coverage executed — its audit table is the baseline this sweep updates.
  Its SSRF residue is `CC-108` (P2, open): schedule it into this sweep rather than leaving it
  floating. Precedent for timing: `CC-105`'s advisory-DB drift turned the audit gate red a week
  after a clean sweep — a sweep goes stale fast.
- `CC-041` (P2) — YubiKey-only 2FA on the GCP project holding the KMS key. Still open, still
  cheap; first step is checking which factors are actually enrolled (the `CC-053` motherboard
  swap may have destroyed a Windows Hello factor). Not a blocker to starting the lifecycle
  stages; must be resolved or explicitly accepted before the gate clears.

**Correctness / fund safety — closed.** `CC-070` (done 2026-08-28 era, chunked getLogs +
deploy-block anchoring), `CC-060` (done, deterministic suite), `CC-046` (done — MCP idempotency,
the "agent retried and we paid twice" failure), `CC-097` (done — set-but-empty env hardening),
`CC-099` (done 2026-08-23, sanctions/PEP screening, PR #136).

**Content / trust — closed.** `CC-029` (Stables removed, Coinbase Australia rewrite), `CC-027`
(README Zero-PII claim), `CC-009` (cleartext waitlist emails), `CC-089` (waitlist dropped).
`CC-036` (P0) is the one content-adjacent item still open — see below.

**Worker experience — closed.** `CC-010`, `CC-021`, `CC-073`, `CC-074`, `CC-011`, `CC-022`
through `CC-024`, `CC-017`, `CC-096`. The residue is `CC-101` (P1, hub) — the 2026-09-01/02
manual walk of the transactional workflow as a worker: evidence submission that only accepts
hand-typed JSON, signature prompts on read-only actions, the agent/poster-side gap. That hub is
**not itself actionable** — its lines must become tickets first. It is a UX-quality gate, not a
fund-safety one: sequence it against the lifecycle stages by judgement, but resolve or explicitly
spin out every line before this hub clears.

**Agent-facing / MCP — closed.** `CC-046` done; `CC-044`/`CC-045` dropped from this gate in the
2026-08-11 pass (P2, distribution ergonomics), and `CC-045` has since closed outright. `CC-102`
(MCP authorization spec conformance, P2) and `CC-108` (webhook SSRF, P2) are open P2s routed to
the day-of sweep, not lifecycle blockers.

**Infrastructure**
- `CC-040` (P1) — the *operational* half only, day-of: Sentry (or a Vercel log drain), external
  uptime against `/api/health`, on-chain event monitoring, runbook per alert. The invariant
  half moved to `CC-085` (done) and got its alert semantics fixed by `CC-104`. Re-point at
  mainnet addresses is `CC-039`'s, not this hub's.
- `CC-033` — **closed wontfix 2026-09-16** (#217): the design pass ran, and the requester gas
  stake is superseded by the ADR-0001 architecture (verdicts are signatures the platform hands
  over; the worker pulls payment). Do not reinstate.
- `BASE_SEPOLIA_RPC_URL` — still the shared root cause it always was, now one `gh secret set`
  away once a provider URL exists (PO action, tracked on `CC-104`). Free-endpoint rate limiting
  is what makes the TRAN alert class fire; it will also slow every lifecycle stage's event reads.
- `CC-048` (P3, self-hosted Sepolia node) — remains the local fallback if a paid endpoint is
  ever unwanted; not a gate.
- `CC-090` (P1, in-progress) — signer/owner separation. Sepolia half mid-flight: the audit
  tooling landed (#213) and checks the verdict signer against the committed `.pub` (or
  `--signer=`), no longer conflating it with the owner. Remaining: rotate the on-chain
  `setVerdictSigner` on Sepolia and prove a verdict still verifies. Its mainnet legs sequence
  inside `CC-034`/`CC-039`, after the deploy transaction, before any funding.
- `CC-036` (P0) — EAS attestations. `ADR-0008` (2026-08-31) settled the design: off-chain
  signature by the verdict signer, worker registers on-chain if they want it, hashes and public
  facts only. Schema + offline UID derivation + verification shipped with 9 tests. Remaining:
  live issuance on real completions and removing the honest inline caveat from the `/learn` and
  homepage copy. Exercises naturally off the back of `CC-078`.
- `CC-091` (P1, in-progress) — succession/DR. Design settled by `ADR-0006`; what remains are
  custody decisions only Aaron can make (which keys live on which Tangem cards). **Not a gate
  on this hub** — it gates `CC-039`'s funding step — but it is flagged for PO attention because
  it is decision-blocked, not work-blocked.
- `CC-072` (P1) — reputation staking, never executed once. Unblocked since `CC-082` landed but
  never started; the 7-day unstake cooldown makes it the longest-tail item here. **Start it
  alongside the earliest lifecycle stage**, not after them.

**Smoke test**
Full register → offer/accept → fund → notify → deliverable → settlement → payout cycle, plus
the dispute → `resolve_dispute` path in both directions, against the live Production Sepolia
deployment — decomposed into `CC-032`/`CC-077`/`CC-078`/`CC-079` so progress is visible. When
all four plus `CC-072` are green, this hub is one day-of sweep away from clear.

This proves Sepolia is ready for the mainnet attempt in `CC-039` — it is **not** the smoke test
that precedes opening to the public; that one happens on mainnet, at the end of `CC-039`.

## What's already done, feeding this gate

Everything in the Closed section of `docs/backlog/INDEX.md` with `epic: public-launch` or
`mainnet` provenance — at this point the substantial majority of the original gate list,
including the entire 2026-08-13 ADR batch (`CC-083`–`CC-089`) and the v2 app-layer batch
(`CC-092`–`CC-095`).

Related: `CC-039` (the mainnet hub this one gates — reconciled the same day as this pass;
its Prerequisite line carries this hub's own remaining-item list).

---

## Update 2026-09-16 — hub reconciliation against the repo (this run)

The gate text above was re-derived against `docs/backlog/` and the repo at master 4a95c30.
This section records what changed and why; the previous gate list is summarised below it.

### Gate status, re-derived

Of the 2026-08-11 list's gates: **closed** — `CC-082`, `CC-080`, `CC-081`, `CC-037`, `CC-083`,
`CC-085`, `CC-086`, `CC-087`, `CC-088`, `CC-089`, `CC-070`, `CC-060`, `CC-046`, `CC-029`,
`CC-027`, `CC-009`, `CC-010`, `CC-021`, `CC-073`, `CC-074`, `CC-011`, `CC-022`–`CC-024`,
`CC-017`, `CC-045`. **Still open** — `CC-032`, `CC-077`, `CC-078`, `CC-079` (lifecycle),
`CC-072` (staking, never executed), `CC-084` (live proof), `CC-104` (PO-blocked residues),
`CC-036` (live issuance), `CC-040` (day-of), `CC-041` (P2), `CC-101` (UX hub), `CC-090`
(Sepolia rotation), the day-of security sweep, and the `BASE_SEPOLIA_RPC_URL` secret.

### What the old list got wrong, now corrected

- **`CC-033` was listed as a pending design pass** — it closed `wontfix` the same day as this
  reconciliation (#217): the design pass ran and the stake is superseded by the
  `ADR-0001` architecture. Removed from the gates.
- **The Funding stage described x402** (`request_human_work` → x402 → `escrow.createTask`) —
  that mechanism was removed by `CC-081` Defect 1 and must never come back. The gate now
  describes the offer/accept → `/api/fund-task` flow that actually exists.
- **`CC-036` was described as "blocked in practice behind `CC-070`"** — `CC-070` has been done
  for weeks, and `ADR-0008` (2026-08-31) settled the design with the schema and verification
  already shipped. What remains is live issuance, not a blocker fight.
- **`CC-078` was still marked `blocked` behind `CC-080`** — unblocked and rescoped 2026-08-28:
  settlement is a worker pull-payment claim, not a platform push.
- **The 2026-08-11 list's "What's already done" section is years stale in spirit** — it predated
  the entire v2 app layer (`CC-092`–`CC-095`) and the ADR-0003 monitor stack. Both batches are
  now recorded as feeding this gate.
- **Post-list arrivals given gate lines**: `CC-101` (2026-09-02 worker-experience hub),
  `CC-104` (2026-09-09 alert classification), `CC-090` (signer/owner separation), `CC-091`
  (flagged, not gated — decision-blocked on PO custody calls, gating `CC-039` not this hub).

### Not done here, deliberately

- No status flips on any ticket other than this hub's `updated` bump — every gate-line change
  above is a *description* correction; the underlying tickets already carry their own states.
- `CC-101`'s decomposition into individual tickets is that hub's own next step (per its body
  and `docs/backlog/README.md`'s numbering rule), not this run's.
- The PO-blocked items (`CC-104`'s workflow scope + RPC secret, `CC-091`'s custody decisions,
  `CC-098`'s lawyer answer on the `CC-039` side) are recorded where they live; none can be
  advanced by a merge.

---

## Update 2026-08-11 — backlog triage pass (superseded gate list)

Lowered P0 → **P1**. Hubs are not actionable work, and carrying P0 on a checklist inflated
the P0 count without pointing at anything anyone could pick up. Now matches `CC-039`, so the two
milestone hubs read consistently on the board.

The gate list itself was rewritten in the same pass. As of that pass: blockers `CC-082`/
`CC-080`/`CC-081`; ADR batch `CC-083`–`CC-089`; security `CC-037`, `CC-041`, day-of sweep;
correctness `CC-070`, `CC-032`, `CC-077`, `CC-078` (blocked), `CC-079`, `CC-072`, `CC-060`;
content `CC-029`, `CC-036`, `CC-027`, `CC-009`; worker experience `CC-010`, `CC-021`, `CC-073`,
`CC-074`, `CC-011`, `CC-022`–`CC-024`, `CC-017`; infrastructure `CC-040`, `CC-033`,
`BASE_SEPOLIA_RPC_URL`; MCP `CC-046`, `CC-044`/`CC-045` dropped to P2. Superseded by the
re-derived list above — kept for the reasoning trail, not as instructions.
