---
id: CC-018
title: Delete dead files from the public repo
status: todo
priority: P2
area: cleanup
epic: public-launch
created: 2026-07-25
updated: 2026-08-11
---
# CC-018 — Delete dead files from the public repo

- `src/app/page_old.tsx` — 107 lines, unreachable under the App Router
- `src/app/page.module.css` — imported by nothing

The repo is public and the footer links to it. Fix: delete both, confirm the build still passes.

---

## Update 2026-08-11 — backlog triage pass

Reviewed in the triage pass — **stays P2**, scope **widened**.

Both listed files confirmed still present: `src/app/page_old.tsx` and `src/app/page.module.css`.

Add to the deletion list: `public/` still contains the untouched Next.js starter SVGs —
`file.svg`, `globe.svg`, `next.svg`, `vercel.svg`, `window.svg`. Only `base-mark.svg` is
actually the project's. These are the same leftovers `CC-012` notes when observing there is no
share image, so the two issues touch the same directory and could be done together.
