---
id: CC-031
title: Tip jar is dead on mobile and points at a Sepolia wallet
status: done
priority: P2
area: frontend
epic: public-launch
created: 2026-07-25
updated: 2026-08-25
---
# CC-031 — Tip jar is dead on mobile and points at a Sepolia wallet

`src/components/BuyMeACoffee.tsx:75,105-107` requires a connected wallet, so it is unusable
on mobile for the same reason as CC-001. It also targets a real address
(`NEXT_PUBLIC_TIP_WALLET_ADDRESS`) while the app runs on Sepolia, so any tip would be test
USDC.

Separately, `.github/FUNDING.yml` sets `ko_fi: wahzammo`, which only affects the GitHub
sponsor button and is unrelated to the on-site jar.

Fix: either hide the tip jar until mainnet, or make the testnet caveat explicit. Decide
alongside CC-007.

---

## Update 2026-08-11 — backlog triage pass

Reviewed in the triage pass — **stays P2**, and the open question in the Fix section is now
**decided: hide the tip jar until mainnet.**

Aaron's call. Rationale: it removes the problem rather than caveating it, and it is consistent with
how `CC-007` was handled — since the coming-soon gate does not lift until after the mainnet
migration, there is no window in which the public sees a tip jar pointing at a Sepolia wallet. A
testnet caveat would be copy written to be deleted.

So the work here is: remove `BuyMeACoffee` from the UI now, and restore it as part of `CC-039`'s
content pass once real USDC is live — at which point the mobile wallet-connection requirement
(`src/components/BuyMeACoffee.tsx:75,105-107`) also needs revisiting on its own merits.

`.github/FUNDING.yml`'s `ko_fi: wahzammo` is unaffected, as this issue already notes.

---

## Update 2026-08-25 — done

Implemented the 2026-08-11 decision: the tip jar is removed from the UI with no caveat, per
Aaron's call.

- `src/components/Footer.tsx` no longer imports or renders `BuyMeACoffee`; a comment at the
  former mount site points here and at CC-039.
- `src/components/BuyMeACoffee.tsx` and `.module.css` are **kept in the repo** (unmounted) for
  restoration; a comment at the top of the component says the same. No other UI mount existed.
- No Footer CSS change was needed — `.inner` is a column flex with a shared `gap`, so nothing
  assumed the jar's space.
- `.github/FUNDING.yml` untouched, per scope.

**Restoring the jar — and revisiting its connected-wallet requirement
(`src/components/BuyMeACoffee.tsx:78,108-110`, the mobile problem from CC-001) — belongs on
`CC-039`'s content pass**, not here. The component also still reads
`NEXT_PUBLIC_TIP_WALLET_ADDRESS`, which must be repointed at a mainnet address before it is
remounted.

Done in feat/cc-031-hide-tip-jar — squash-merged via the PR referencing CC-031 (the branch
commit is `2b7859e`; `git log --grep=CC-031` reconstructs the story either way).

`npm test` (618 passed), `npm run lint` (0 errors; one pre-existing warning in
`src/app/api/basedhuman.mcp/route.ts`, untouched), `npm run typecheck` and `npm run build`
all green.
