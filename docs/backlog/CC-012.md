---
id: CC-012
title: No OpenGraph or Twitter metadata — every shared link renders as a blank card
status: done
priority: P2
area: content
epic: public-launch
created: 2026-07-25
updated: 2026-08-25
---
# CC-012 — No OpenGraph or Twitter metadata — every shared link renders as a blank card

## Problem
`src/app/layout.tsx:13-17` sets title and description only. There is no `metadataBase`,
no `openGraph`, no `twitter`, and no share image — `public/` holds `base-mark.svg` plus
leftover Next.js starter SVGs.

Since the entire plan is to promote the link, this is directly load-bearing: every post on
LinkedIn, X or Slack will show a grey box.

## Fix
Add `metadataBase`, `openGraph` and `twitter` blocks in `layout.tsx`, plus a 1200x630 image
in `public/`. Validate with a card debugger before promoting.

---

## Update 2026-08-11 — backlog triage pass

Lowered P1 → **P2**. Not a change of view on the substance — share cards genuinely matter
for promotion — but on *when* it matters. Per the locked launch sequence, the public's first look at
the site is on mainnet (`CC-039`), so nothing is shared or indexed until then. Same treatment as
`CC-013`, and the same reasoning that moved `CC-007` to `blocked`.

Sequence this into `CC-039`'s content pass rather than `CC-076`.

---

## Done 2026-08-25 — feat/cc-012-opengraph-twitter-metadata

`src/app/layout.tsx` now exports `metadataBase` (`https://carbon-contractors.com` — a
constant; the hostname already appears verbatim in the wallet-challenge message and the
MCP docs, and no site-URL env var exists), an `openGraph` block (`website`, `siteName`,
`url: "/"`, 1200x630 image) and a `twitter` block (`summary_large_image`). Title and
description are unchanged from the existing layout copy.

`public/og.png` — 1200x630, dark (`#0A0B0D` from the design tokens), monospace, built with
ImageMagick + DejaVu Sans Mono from `public/base-mark.svg` (rasterised, no new logo). No
new npm dependency; no leftover starter SVGs (there were none to remove at `c1fc229`).

Verified: `npm test` (618), lint, typecheck, and `npm run build` all green; the
prerendered `.next/server/app/index.html` carries the full `og:*` and `twitter:*` tag set
with absolute image URLs. **Deferred:** card-debugger validation against a live public URL
— the site is behind the coming-soon gate, so nothing is scrapable. Deferred to the
`CC-039` content pass / first public share; also note `metadataBase` should move into
`config.ts` if a site-URL env var is ever introduced.
