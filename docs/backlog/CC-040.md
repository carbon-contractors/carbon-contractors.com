---
id: CC-040
title: Production monitoring and alerting
status: todo
priority: P1
area: infra
epic: public-launch
linear: NOR-305
created: 2026-07-25
updated: 2026-08-13
---
# CC-040 — Production monitoring and alerting

Migrated from NOR-305. Nothing currently watches production — the site was up for four
months unattended and the only reason that was survivable is that no real money was moving.

## Scope
- Error alerting: Vercel log drain, or Sentry
- On-chain: `CarbonEscrow` event monitoring, KMS signing failures, deployer ETH balance
  (the balance alert becomes moot if CC-033 lands)
- Uptime: external monitor against `/api/health`, which already reports database, escrow
  contract and session health
- A runbook per alert type, so a 2am page has a documented response

Related: CC-033, CC-039.

---

## Update 2026-08-10 — reclassified: public-launch, not mainnet

"The site was up for four months unattended" already happened on testnet — real users hitting an
unmonitored site is a launch-day risk, not something that waits for mainnet. `CC-058` (Supabase
tier) is already done, which removes one failure mode this would have needed to catch; the rest
of the scope here (error alerting, on-chain event monitoring, uptime) stands.

## Update 2026-08-10 (2) — partially covered by paid tiers; the rest is a day-before-launch task

Aaron: Vercel Pro and Supabase Pro (both purchased this session — see `CC-058`, `CC-063`) already
cover part of this scope — Vercel Pro includes better logging/observability than Hobby, Supabase
Pro includes daily backups and a proper dashboard, and the "unattended for four months" failure
mode specifically (`CC-058`) is closed.

**Not covered by the paid tiers, still needed:** error alerting actually wired up (a Vercel log
drain or Sentry, not just logs existing to be read manually), on-chain event monitoring
(`CarbonEscrow`, KMS signing failures), and the runbook.

Sequencing: this is a **day-before-launch** task, same as `CC-035` — set up monitoring right
before the gate lifts, not weeks earlier where it would just be watching an empty coming-soon
page.

---

## Update 2026-08-11 — backlog triage pass

Reviewed in the triage pass — **stays P1**, now explicitly flagged as a **day-of** task,
the same sequencing as `CC-035`.

The distinction matters for reading the board: this is P1 because it is genuinely required, not
because it is actionable now. Setting up alerting weeks early just means watching an empty
coming-soon page, and the mainnet migration will re-point every address and network var anyway.

Scope still outstanding, per the 2026-08-10 updates: error alerting actually wired up (a Vercel log
drain or Sentry, not merely logs existing to be read by hand), on-chain event monitoring for
`CarbonEscrow` and KMS signing failures, and the per-alert runbook. Vercel Pro and Supabase Pro
cover the rest, and `CC-058` closed the "unattended for four months" failure mode.

Two dependencies to honour when it is picked up: `CC-033` removes the need for a deployer ETH
balance alert if it lands first, and `CC-051` wants the threshold instrumentation (peak concurrent
USDC escrowed per funding agent) fed in here.

---

## Update 2026-08-13 — split by `ADR-0003`; this ticket keeps the operational half

`ADR-0003` rescopes and splits this issue. The correctness half is now **`CC-085`**.

**What moved out.** Everything protecting correctness, fund safety or a published claim — the
invariant monitors — and it moved *ahead* of `CC-077`/`078`/`079` rather than staying day-of. The
reasoning: the ADRs introduced failure modes that are **silent**, where the system keeps working and
produces wrong outcomes without erroring. The lifecycle tests are the next real work item, and the
invariant monitors are how you know the lifecycle actually worked.

**What stays here.** Availability monitoring, and this issue's "day-before-launch, or you're just
watching an empty coming-soon page" reasoning remains correct for it: Sentry for application errors,
an external uptime monitor against `/api/health`, on-chain event monitoring, and re-pointing at
mainnet addresses under `CC-039`.

**Scope reductions from `ADR-0001` Amendment 1**, recorded so they are not reinstated from the older
text above:

- **No platform transaction monitoring in the settlement path** — post-Amendment 1 the platform does
  not transact at all. Verdicts are EIP-712 signatures handed to the parties, and the worker claims.
- **Deployer ETH balance alerting is moot for settlement.** It remains relevant only for deployment
  and ownership operations. (Already flagged here as dependent on `CC-033`.)
- **"KMS signing failures" shrinks** from an on-chain failure monitor to `verify-signer` — a health
  check that the signer can produce a signature at all. That lives in `CC-085`.

The `CC-051` threshold instrumentation noted above also moves to `CC-085`, as an eighth monitor.

Priority stays **P1**, still day-of.

Related: `ADR-0003`, `CC-085`, `CC-086`, `CC-039`, `CC-033`, `CC-051`.
