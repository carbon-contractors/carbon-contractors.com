---
id: CC-015
title: Waitlist has no honeypot or captcha
status: todo
priority: P2
area: security
epic: public-launch
created: 2026-07-25
updated: 2026-08-13
---
# CC-015 — Waitlist has no honeypot or captcha

`src/app/api/waitlist/route.ts` validates the email shape and upserts with
`ignoreDuplicates`, but the only throttle is the shared 60/min/IP middleware bucket across
all of `/api`. Once the site is promoted, a trivial script can inject thousands of fake
addresses and poison the one asset worth having.

Fix: add a honeypot field plus a dedicated tighter rate limit for this route. Consider
double opt-in, which also helps the Spam Act position in CC-008.

---

## Update 2026-08-11 — backlog triage pass

Reviewed in the triage pass — **stays P2**. See `CC-020`'s note: this issue's only
current throttle is that per-instance in-memory bucket, so the two should be decided together. If
`CC-020` is resolved by correcting the comment rather than building the Upstash limiter, this issue
needs its own dedicated route limit rather than relying on the shared one.

The double opt-in option also interacts with `CC-008`'s Spam Act position and with `CC-016`
(closed `wontfix` — the waitlist is read through the Supabase dashboard).

---

## Update 2026-08-13 — expected to close `wontfix` when `CC-089` lands

`ADR-0002` handover step 9 drops the `waitlist` table and removes the coming-soon capture form
entirely, as the intended action at launch. There is then no form to protect and no table to poison,
so this issue has no remaining subject.

Not closing it yet — the form still exists today, and if the drop is deferred this becomes live
again. Close as `wontfix` with that reason once `CC-089` ships.

Related: `ADR-0002`, `CC-089`, `CC-020`.
