---
id: CC-019
title: Hamburger bars disappear when the mobile menu opens
status: done
priority: P2
area: frontend
epic: public-launch
created: 2026-07-25
updated: 2026-08-21
---
# CC-019 — Hamburger bars disappear when the mobile menu opens

`src/components/NavBar.tsx:37-39` uses `className={menuOpen ? styles.barOpen : styles.bar}`,
which **replaces** the base class rather than appending it. `.barOpen`
(`src/components/NavBar.module.css:84-92`) declares no width, height or background, so the
bars vanish instead of forming an X.

Fix: `className={`${styles.bar} ${menuOpen ? styles.barOpen : ""}`}`.

---

## Update 2026-08-11 — backlog triage pass

Reviewed in the triage pass — **stays P2**. One-line fix, unchanged and still valid.

---

## Done 2026-08-21 — commit `ad1c930`

Applied the fix as written: `className={`${styles.bar} ${menuOpen ? styles.barOpen : ""}`}`
on all three spans. Confirmed against `NavBar.module.css` that the open-state rules are
`.barOpen:nth-child(n)` selectors that only set transform/opacity, so the combined classes
give the bars their base geometry plus the X animation.
