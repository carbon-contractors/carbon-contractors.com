---
id: CC-090
title: Separate the verdict signer from the contract owner
status: in-progress
priority: P1
area: contracts
epic: public-launch
created: 2026-08-15
updated: 2026-09-16
---

# CC-090 — Separate the verdict signer from the contract owner

`CarbonEscrow` v2 was deployed on 2026-08-15 with the HSM key
`0xa8931097540e69B474013D294d0bA6A2cC853e4b` as **both** the contract owner and the sole
accepted verdict signer. One key therefore signs verdicts, arbitrates disputes, and manages
the signer set.

## Why this is P2 and not P0

**It grants no authority the platform does not already hold.** Vercel can already act as the
owner via Workload Identity Federation, and `resolveDispute` is `onlyOwner`. Anyone who could
forge a verdict through that path could instead call `resolveDispute` directly, which is
strictly more powerful — a forged verdict only moves a task to `Disputed`, whereas
`resolveDispute` settles it. Separating the keys buys nothing against that threat model.

So this is not a live weakness. It is a constraint on a *future* posture.

## When it starts to matter

The moment the owner moves somewhere colder than a Vercel-reachable key — a multisig, a
hardware wallet, an offline signer. At that point verdict signing must stay hot (it happens on
every settlement) while ownership does not, and one key cannot be both.

`ADR-0001` D9's v2 step points the same way: verdicts become permissionless against a bond, at
which point the accepted-signer set has several members and the owner being one of them is
incidental rather than structural.

## Scope

- A second KMS key (or key version) for verdict signing, with its own IAM grant and its own
  WIF binding.
- `VERDICT_SIGNER_ADDRESS` points at the new key; `setVerdictSigner(new, true)` then
  `setVerdictSigner(old, false)`.
- `docs/Security-Trust-Disclosure.md` updated — it currently describes one key.
- `scripts/audit/verify-escrow-deployment.mjs` checks the signer against the *expected signer*
  rather than against the HSM owner constant, which is what it does today.

## No redeploy required

This is the reason the accepted-signer set is an owner-managed mapping rather than a
constructor-baked immutable. The whole change is two `setVerdictSigner` calls plus key
provisioning — deliberately designed to be a set change, not a migration.

## A second, stronger argument — added 2026-08-15

`CC-091` (succession and DR) needs **the same change** for a different reason. `resolveDispute` is
`onlyOwner` with no timeout and no fallback, so if the owner key becomes unreachable — not
compromised, just unreachable — every task in `Disputed` or `Arbitrating` is stranded permanently.
A multisig owner turns that from a legal problem into an operational one.

The P2 above was reasoned purely on the compromise threat model, where separation genuinely buys
nothing while both roles sit on one Vercel-reachable key. **That reasoning does not cover
unavailability, and unavailability is the stronger case.** Priority is Aaron's call; flagging that
it was set without this argument in view.

Related: `CC-082`, `CC-059`, `CC-091`, `ADR-0001` D9, `CC-039` (mainnet — worth deciding before the
one-way migration, since mainnet is where a colder owner key actually earns its cost).

---

## Update 2026-08-26 — re-rated P1, and the target architecture is now decided

**P2 → P1.** `ADR-0006` D2 re-rates this, and was accepted 2026-08-26. The original P2 reasoning
still holds on its own terms — separation buys little against a *compromise* while both roles sit on
one key — but succession is a second and stronger argument for the same change, and it is the one
that sets the priority.

### Accepted target architecture

| Role | Custody |
| :-- | :-- |
| **Contract owner** | **2-of-4 Safe**, four hardware-isolated keys: each Tangem card initialised **on its own**, link step not run (`ADR-0006` A2). Custody: Aaron two (separate buildings), two family members one each. Slots 3 and 4 are transitional (`ADR-0006` D11). |
| **Verdict signer** | a single GCP Cloud KMS / HSM key, unchanged — it signs automatically and holds no custody |

Losing the verdict signer **fails safe**: no verdicts means every delivered task resolves to the
worker after the review window (`ADR-0001` D6). That asymmetry is why the two roles want different
custody models rather than the same one twice.

### The acceptance test is on-chain

**Four cards are only four keys if each is initialised on its own.** Cards linked to one key have the
security of a 1-of-1 and are indistinguishable from a real multisig on a block explorer.

Corrected 2026-08-31 (`ADR-0006` **A2**): this paragraph said Tangem "sells cards as a set that shares
one seed by default" and that buying separately closed the risk by construction. Neither holds — cards
arrive uninitialised, and linking one to another's key is an explicit setup step with its own screen.
**Hardware ordered 2026-08-31 as two multi-card packs, expected ~2026-09-17.** → **Arrived 2026-09-07, six days early. Execution unblocked.** Runbook's superseded 2-of-3/D3-mechanism text synced to this ticket's architecture in PR #196; default-board IRL cards updated to the execution checklist.

So the requirement is a setup procedure, and a procedure performed once is a claim. This ticket does
not close on it having been performed. It closes on:

- [x] **Four distinct addresses read off the four cards, before they are distributed** (`ADR-0006`
      A2.2). Free, needs no Safe and no transaction. After distribution the cards are in different
      buildings and in non-technical hands, which is the expensive moment to discover a repeat.
      **Done 2026-09-09** — read via Megnat (third-party Tangem reader; Play-delisted, APK
      `747087b0…859cd` archived at `workspace/tangem/` on frankenfarm), all four EIP-55 valid and
      distinct, all four verified virgin on Base mainnet (nonce 0, no code) before any use.
      **Tooling correction (`ADR-0006` A2.3):** the gen-2 Tangem app refuses to *display* any wallet
      that is not part of a linked backup group, so the census could not have been done in the
      official app. Card↔address mapping lives in `workspace/tangem/tangem-cards.json`
      (frankenfarm, not committed — contains no secrets, but keeping the record off-repo until
      custody is assigned avoids baking a card/holder table into git history).
- [ ] Four Safe owners at **four addresses with no shared derivation**, read from the chain.
      **Testnet rehearsal 2026-09-09:** Safe v1.4.1+L2 `0x7b4B…52A48` deployed on Base Sepolia
      (deploy tx `0xbc00df…ac74f`), owner set and threshold read back from the contract: 4 owners,
      threshold 2, exact match to the census, proxy slot 0 → canonical singleton. **Three
      transactions executed on three different pairs — {1,4}, {3,1}, {2,3} — and all four cards'
      signatures were accepted by Safe's transaction service**, which validates every confirmation
      against the on-chain owner set. That is the "four cards are four keys" check, done the
      expensive-but-honest way: three quorums, no pair repetition, every key exercised. Execution
      used the relayer pattern (unrelated EOA broadcasts a fully-signed tx) — the same pattern the
      platform will use in production, rehearsed here first.
- [ ] The Safe is the `CarbonEscrow` owner — `verify-contract-owner.mjs` passes against it.
      *(Mainnet leg; ordered after `CC-034`'s redeploy.)*
- [ ] The accepted verdict signer is **not** any Safe owner — `verify-signer.mjs` passes, and the two
      scripts together assert the separation. *(Mainnet leg.)*
- [ ] `chain-constants.json` `verdictSignerSeparation.separated` flips to `true`, with the owner
      block updated. *(Mainnet leg.)*
- [x] **A 2-of-4 signature has executed one owner-only call on testnet.** **Done 2026-09-09** —
      three times over (nonces 0/1/2, txs `0x3e6cef…73033`, `0x4dc4b6…37322d`, `0xeaec7e…72a7b5`),
      including owner-only `execTransaction` calls validated by the Safe singleton itself.
      Rehearsal Safe holds 0.0002 ETH and nonce 3; full record in
      `workspace/tangem/safe-sepolia.json`.
- [ ] **The succession path specifically: a 2-of-4 signature by the two family keys alone.** Not one
      that merely includes them. If Aaron is gone, family-only is the only path there is, and both
      holders are non-technical — an untested succession path is a designed one, not a working one.
      **Status 2026-09-09: not yet satisfied and not closable on tonight's pairs.** The three
      executed pairs were {1,4}, {3,1}, {2,3} — every family-pair candidate *includes* one of the
      cards tentatively designated family, but no executed tx was signed by a designated family
      pair alone. **The criterion is now satisfiable by custody assignment:** cards 2+3 (executed
      nonce 2 with no other signature) or cards 1+4 (executed nonce 0) each have on-chain evidence
      of a working pair; assign one of those pairs as "family" and the criterion is met by the
      already-executed transaction. Recommendation before distribution: family = cards 2+3, and
      record the assignment in `ADR-0006` when made.

### Ordering

Before `CC-034`, and **after** the mainnet deployment transaction — a redeploy resets ownership to the
deployer (`CC-082`). Scoped into `CC-034`'s milestone 2026-08-26.

**Custody of the three keys is an open `ADR-0006` item, not an engineering task.** Three cards in one
person's hands delivers loss resistance; it does not deliver succession, which is D2's stated purpose.
This ticket can complete the *architecture* while that remains unanswered — and if it does, the
succession claim must not be recorded as satisfied.

Related: `ADR-0006` D2 + open items, `CC-034`, `CC-059`, `CC-082`, `docs/BCP-DR.md`
(*Contract ownership*), `chain-constants.json`.

---

## Update 2026-09-09 — rehearsal night: testnet half complete, distribution gated on two items

**Done tonight** (details in the checklist above): address census with reader-independence proofs,
Base Sepolia Safe deployed and verified, three 2-of-4 executions across three pair combinations,
all four cards' signatures validated against the on-chain owner set, relayer execution pattern
proven. Working records on frankenfarm under `workspace/tangem/` (not committed): the census, the
deployer key (testnet-only), the rehearsal ledger, the archived Megnat APK + SHA-256.

**Not done, in dependency order:**

1. **ACR1552U reader ordered & arrived** — from a traceable channel (Mouser AU / element14; the
   ACR122U on Amazon is end-of-life and the common listing is a counterfeit profile,
   `ADR-0006` A3.2).
2. **Desktop admin rig built** (Tangem SDK over PC/SC on frankenfarm) → set **four distinct access
   codes**, re-verify each card's address, physically label cards (address suffix on the back).
3. **Custody assignment** — family pair = cards 2+3 (recommended; on-chain evidence at nonce 2),
   Aaron pair = 1+4 (nonce 0). Recorded in `ADR-0006` when made; closes the succession criterion.
4. Mainnet legs remain sequenced behind `CC-034`'s redeploy, unchanged.

---

## Update 2026-09-16 — audit-tooling leg shipped: verify-escrow-deployment checks the signer, not the owner

The scope item *"`scripts/audit/verify-escrow-deployment.mjs` checks the signer against the
expected signer rather than against the HSM owner constant, which is what it does today"* is
now **done** (PR pending, this update rides in it). The script previously read
`acceptedSigners(HSM)` — the owner constant — which after separation would fail against a
correct deployment and could never detect a signer==owner regression at all, because it
asserted the two addresses were the same one.

What the script does now:

- Expected signer = the committed `.pub` derivation (offline), or `--signer=0xADDR` during a
  rotation window, with `VERDICT_SIGNER_ADDRESS` cross-checked and a mismatch **fatal** (the
  deploy script seeds the accepted set from that env var — a stale one means every verdict is
  silently rejected; this is the CC-090 rotation trap made visible).
- `--owner=0xADDR` lets the mainnet leg verify against the Safe without editing the constant;
  the default remains the CC-059 HSM key with a pointer in the failure text.
- A visible `signer/owner separation` line on every run: `SAME key` vs `separated`. On
  **mainnet**, signer == owner is now a hard exit-1 gate in this script (ADR-0006 D2) — so the
  "two scripts together assert the separation" criterion below is now enforced by tooling, not
  by remembering to look.

Verified live against the Sepolia escrow (`0xc6aa99…E4d3`, 2026-09-16): clean run exit 0 and
correct `SAME key (CC-090 open — testnet posture)` reporting; rotation-window (`--signer` →
unaccepted address) and env-mismatch paths both exit 1 with the right diagnostics. Test suite
829/829, typecheck and lint clean.

This does **not** tick any unchecked box above — the mainnet legs still wait on `CC-034`'s
redeploy, custody assignment, and the admin rig. It removes the one piece of the mainnet leg
that would otherwise have been written on the day, under time pressure, against a deployment
that already held money.
